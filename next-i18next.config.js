module.exports = {
  i18n: {
    // 支持的语言列表
    locales: ['en', 'zh-CN', 'ja', 'zh-TW'],
    // 默认语言
    defaultLocale: 'en',
    // (可选) 禁用自动区域设置检测
    // localeDetection: false,
  },
  // (可选) 如果你的翻译文件不在 public/locales/[lang]/[namespace].json
  // localePath: path.resolve('./my/custom/path'),
  // (可选) 重新加载翻译的时间间隔（开发模式下）
  // reloadOnPrerender: process.env.NODE_ENV === 'development',
}; 